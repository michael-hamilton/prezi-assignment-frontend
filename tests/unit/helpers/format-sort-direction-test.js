import { formatSortDirection } from 'frontend/helpers/format-sort-direction';
import { module, test } from 'qunit';

module('Unit | Helper | format sort direction');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatSortDirection([42]);
  assert.ok(result);
});
